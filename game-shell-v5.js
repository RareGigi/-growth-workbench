/* 月相庭院 Moon Garden v1 */
(function(){
  const STORE='moon-garden-v1';
  const appData=typeof D==='object'&&D?D:(window.D||{});
  const SCENE='assets/moon-garden/2026-09-guiying-stage4.jpg';
  const STAGES=[
    {n:'朔月',min:0,max:10,line:'雾还没有散，庭院只露出一点轮廓。'},
    {n:'月芽',min:10,max:25,line:'今夜，池边亮起了第一盏灯。'},
    {n:'弦月',min:25,max:45,line:'桂枝有了新的花苞，月光开始落到水面。'},
    {n:'盈月',min:45,max:70,line:'庭院渐渐丰盈，萤火在水边出现。'},
    {n:'望前',min:70,max:90,line:'桂花盛开，灯火已经连成一条回家的路。'},
    {n:'满月',min:90,max:101,line:'满月高悬，这个月的庭院已经完成。'}
  ];
  const THEMES=[
    ['2026-09','桂影照水','桂花','深靛 · 月白 · 暖金','薄雾与桂香','桂枝、石桥、月池','桂香书签','桂影满池，灯火沿桥而亮','桂月·风从水上来'],
    ['2026-10','霜灯晚庭','银杏','墨蓝 · 琥珀 · 霜白','初霜与清冷空气','银杏坡、石灯、长廊','铜铃','霜叶铺径，远灯如星','霜庭·灯火未眠'],
    ['2026-11','雁影茶烟','芦苇','烟灰 · 茶褐 · 银白','风与薄云','水榭、芦岸、茶席','旧信封','雁影掠月，茶烟越过水面','雁月·一封晚信'],
    ['2026-12','雪砚长夜','雪松','夜蓝 · 雪白 · 墨黑','细雪','雪庭、书斋、冰池','白瓷砚','雪覆庭院，书斋一灯独明','雪夜·灯下有书'],
    ['2027-01','梅窗初岁','白梅','绛红 · 月白 · 松墨','寒雾','梅树、窗格、石阶','红绳结','梅枝映月，新岁第一盏灯','梅影·岁初一枝'],
    ['2027-02','雨灯青瓦','山茶','青灰 · 暖黄 · 绯红','春雨','青瓦、雨檐、浅池','纸伞','雨落青瓦，山茶沿阶盛开','雨庭·春声将至'],
    ['2027-03','樱汀微风','樱花','雾粉 · 天青 · 月白','轻风','樱堤、小舟、水汀','透明风铃','樱花越过水面，月光落在小舟','樱月·风来有信'],
    ['2027-04','藤梦回廊','紫藤','藤紫 · 灰蓝 · 香槟金','花影与柔雾','长廊、藤架、镜池','银书扣','紫藤垂满回廊，水面映出月轮','藤月·长廊有梦'],
    ['2027-05','萤野初夏','鸢尾','深青 · 荧绿 · 金黄','萤火与暖风','草坡、溪流、木桥','玻璃萤灯','萤火沿溪而起，庭院进入初夏','萤月·微光成河'],
    ['2027-06','荷风入夜','荷花','黛青 · 荷粉 · 珍珠白','水汽','荷池、亭台、浮叶','荷叶杯','满池荷花与满月相对','荷月·水心见月'],
    ['2027-07','星河凉席','夜来香','群青 · 银紫 · 冷白','清透星空','露台、竹影、星桥','星图纸','银河低垂，夜来香沿栏开放','星月·长夜有光'],
    ['2027-08','海棠晚照','海棠','暮蓝 · 胭脂 · 金橙','晚霞余温','海棠坡、月门、浅溪','琉璃坠','夏末海棠与新月交替，年度月轮完成','海棠月·一年归庭']
  ].map(x=>({key:x[0],name:x[1],plant:x[2],palette:x[3],air:x[4],elements:x[5],item:x[6],final:x[7],card:x[8]}));
  const ICONS={garden:'<path d="M7 26c5-9 12-13 20-14-2 8-7 13-15 14M16 27V15"/>',cal:'<rect x="6" y="8" width="20" height="18" rx="2"/><path d="M10 5v6M22 5v6M6 13h20"/>',book:'<path d="M7 7c4-2 7-1 9 2v18c-3-3-6-4-9-2Zm18 0c-4-2-7-1-9 2v18c3-3 6-4 9-2Z"/>',archive:'<rect x="7" y="6" width="18" height="21" rx="2"/><path d="M11 11h10M11 16h10M11 21h6"/>',back:'<path d="M20 7 11 16l9 9M12 16h14"/>',leaf:'<path d="M25 7C15 7 8 12 8 20c6 1 12-1 17-13ZM8 26c3-7 7-11 14-15"/>'};
  const icon=n=>`<svg viewBox="0 0 32 32" aria-hidden="true">${ICONS[n]||ICONS.garden}</svg>`;
  const old=document.getElementById('gameAppV5'); if(old)old.remove();
  const app=document.createElement('section'); app.id='gameAppV5'; app.className='gameAppV5 mgApp'; document.body.appendChild(app);
  let state={view:'garden',selectedDate:null,archives:{},seen:{}};
  try{state={...state,...JSON.parse(localStorage.getItem(STORE)||'{}')};}catch(e){}
  const persist=()=>localStorage.setItem(STORE,JSON.stringify(state));
  const dayKey=d=>{const z=new Date(d);return `${z.getFullYear()}-${String(z.getMonth()+1).padStart(2,'0')}-${String(z.getDate()).padStart(2,'0')}`};
  const today=()=>dayKey(new Date());
  const monthKey=()=>today().slice(0,7);
  const theme=key=>THEMES.find(t=>t.key===key)||THEMES[0];
  function dayMoonlight(k){let v=0; const tasks=(appData.tasks&&appData.tasks[k])||[];v+=tasks.filter(x=>x.done).length*20;v+=((appData.habits&&appData.habits[k])||[]).length*6;v+=(appData.focus||[]).filter(x=>x.date===k).reduce((s,x)=>s+Math.min(30,Math.round((x.m||0)*.65)),0);v+=(appData.study||[]).filter(x=>x.date===k).reduce((s,x)=>s+Math.min(35,Math.round((x.m||0)*.55)),0);v+=(appData.english||[]).filter(x=>x.date===k).reduce((s,x)=>s+Math.min(20,Math.round((x.m||0)*.35)),0);v+=(appData.podcasts||[]).filter(x=>x.date===k).length*18;v+=(appData.wordLog||[]).filter(x=>x.date===k).reduce((s,x)=>s+Math.min(35,Math.ceil((x.n||0)/250)*5),0);if(appData.body&&appData.body[k])v+=8;if(appData.reviews&&appData.reviews[k])v+=10;return Math.min(150,v);}
  function monthDays(key){const [y,m]=key.split('-').map(Number),n=new Date(y,m,0).getDate();return Array.from({length:n},(_,i)=>`${y}-${String(m).padStart(2,'0')}-${String(i+1).padStart(2,'0')}`);}
  function stats(key){const ds=monthDays(key), daily=ds.map(k=>({date:k,val:dayMoonlight(k)}));const total=daily.reduce((s,x)=>s+x.val,0);const target=1800;const pct=Math.min(100,total/target*100);const overflow=Math.max(0,total-target);const active=daily.filter(x=>x.val>0).length;const focus=(appData.focus||[]).filter(x=>x.date&&x.date.startsWith(key)).reduce((s,x)=>s+(x.m||0),0);return{daily,total,target,pct,overflow,active,focus};}
  function stageFor(pct){return Math.max(0,STAGES.findIndex(s=>pct>=s.min&&pct<s.max));}
  function deterministic(key,salt){let h=2166136261;for(const c of key+salt){h^=c.charCodeAt(0);h=Math.imul(h,16777619)}return Math.abs(h>>>0);}
  function unlocked(key,pct){const s=stageFor(pct),t=theme(key);const base=[['月桂新芽','桂树',10],['石灯笼','第一盏灯',18],['水面月影','月池',32],['木棂风铃','风铃',48],['萤火群','萤火',58],[t.item,'本月小物',78],['满月庭院','最终月景',90]];return base.filter(x=>pct>=x[2]).map((x,i)=>({...x,id:`${key}-${i}`,seed:deterministic(key,String(i))}));}
  function freezePast(){THEMES.forEach(t=>{if(t.key<monthKey()&&!state.archives[t.key]){const s=stats(t.key);state.archives[t.key]={key:t.key,total:s.total,pct:s.pct,active:s.active,focus:s.focus,stage:stageFor(s.pct),unlocked:unlocked(t.key,s.pct),frozenAt:today(),seed:deterministic(t.key,'garden')}}});persist();}
  function render(){freezePast();const key=monthKey(),t=theme(key),s=stats(key),stage=stageFor(s.pct);app.innerHTML=`<div class="mgScene" style="--scene:url('${SCENE}');--stage:${stage}"><div class="mgShade"></div><header class="mgTop"><button class="mgBack" data-exit>${icon('back')}</button><div><small>${key.replace('-','.')} · MOON GARDEN</small><h1>${t.name}</h1></div><div class="mgMeta"><span>${STAGES[stage].n}</span><b>${Math.round(s.pct)}%</b><em>${s.total} 月辉</em></div></header><div class="mgMoon phase${stage}"><i></i></div><div class="mgWhisper">${STAGES[stage].line}</div>${renderView(state.view,key,t,s,stage)}<nav class="mgDock"><button data-view="garden" class="${state.view==='garden'?'active':''}">${icon('garden')}<span>庭院</span></button><button data-view="calendar" class="${state.view==='calendar'?'active':''}">${icon('cal')}<span>月相历</span></button><button data-view="record" class="${state.view==='record'?'active':''}">${icon('book')}<span>本月记录</span></button><button data-view="archive" class="${state.view==='archive'?'active':''}">${icon('archive')}<span>收藏馆</span></button></nav></div><div class="mgToast" role="status"></div>`;}
  function renderView(view,key,t,s,stage){if(view==='calendar')return calendarView(key,s);if(view==='record')return recordView(key,s,t);if(view==='archive')return archiveView();return gardenView(key,t,s,stage);}
  function gardenView(key,t,s,stage){const u=unlocked(key,s.pct),next=STAGES[Math.min(5,stage+1)];return `<main class="mgGarden"><section class="mgProgress glass"><small>${STAGES[stage].n} · ${Math.round(s.pct)}%</small><div><i style="width:${s.pct}%"></i></div><p>${s.total} / ${s.target} 月辉${s.overflow?` · 溢出 ${s.overflow}`:''}</p><em>${stage===5?'本月庭院已完成，继续成长会累积稀有印记。':`下一阶段：${next.n}`}</em></section><section class="mgToday glass"><div class="mgPanelHead"><b>今日成长</b><span>+${dayMoonlight(today())} 月辉</span></div>${todayRows(today())}<button data-workbench>回工作台继续成长</button></section><section class="mgElements glass"><div class="mgPanelHead"><b>庭院变化</b><span>${u.length} 项已出现</span></div><div class="mgElementRail">${u.length?u.map(x=>`<button data-element="${x.id}"><b>${x[0]}</b><small>${x[1]}</small></button>`).join(''):'<p>完成今天的第一件事，庭院会留下第一个变化。</p>'}</div></section>${stage===5?`<button class="mgCardCTA" data-freeze>生成本月月相卡面</button>`:''}</main>`;}
  function todayRows(k){const rows=[];((appData.tasks&&appData.tasks[k])||[]).filter(x=>x.done).slice(0,3).forEach(x=>rows.push([x.t,20]));(appData.study||[]).filter(x=>x.date===k).slice(0,1).forEach(x=>rows.push([`学习 ${x.m} 分钟`,Math.min(35,Math.round(x.m*.55))]));(appData.focus||[]).filter(x=>x.date===k).slice(0,1).forEach(x=>rows.push([`专注 ${x.m} 分钟`,Math.min(30,Math.round(x.m*.65))]));return rows.length?rows.map(x=>`<div class="mgRow"><span>${escapeHtml(x[0])}</span><b>+${x[1]}</b></div>`).join(''):'<div class="mgEmpty">今天还没有新的月辉。去完成一件现实里的事。</div>';}
  function calendarView(key,s){const [y,m]=key.split('-').map(Number),first=new Date(y,m-1,1).getDay();return `<section class="mgSheet"><div class="mgSheetHead"><small>MOON CALENDAR</small><h2>${m}月月相历</h2><p>没有完成的日子不会被标记为失败，只会是一轮安静的小月亮。</p></div><div class="mgWeek">${['日','一','二','三','四','五','六'].map(x=>`<b>${x}</b>`).join('')}</div><div class="mgCal">${'<span></span>'.repeat(first)}${s.daily.map((x,i)=>`<button data-date="${x.date}" class="${x.val?'lit':''}"><i style="--wax:${Math.round((i+1)/s.daily.length*100)}%"></i><b>${i+1}</b><small>${x.val||''}</small></button>`).join('')}</div>${state.selectedDate?dayDetail(state.selectedDate):''}</section>`;}
  function dayDetail(k){const v=dayMoonlight(k);return `<div class="mgDayDetail glass"><b>${k.replaceAll('-','.')} · ${v} 月辉</b>${todayRows(k)}<small>${v?`这一天的努力已经写进庭院。`:'这一天很安静，也没有关系。'}</small></div>`;}
  function recordView(key,s,t){const u=unlocked(key,s.pct);return `<section class="mgSheet"><div class="mgSheetHead"><small>MONTHLY NOTES</small><h2>${t.name} · 本月记录</h2><p>${t.final}</p></div><div class="mgRecordStats"><article><small>完成天数</small><b>${s.active}</b></article><article><small>累计月辉</small><b>${s.total}</b></article><article><small>专注时间</small><b>${s.focus} min</b></article><article><small>庭院元素</small><b>${u.length}</b></article></div><div class="mgTimeline">${u.map((x,i)=>`<article><i></i><div><small>${x[2]}% 解锁</small><b>${x[0]}</b><p>${x[1]}随着这个月的成长出现，并会永久留在本月庭院。</p></div></article>`).join('')||'<p>本月第一项变化还在等待你。</p>'}</div></section>`;}
  function archiveView(){const cards=THEMES.map(t=>{const isPast=t.key<monthKey(),cur=t.key===monthKey(),a=state.archives[t.key];return `<button class="mgArchiveCard ${isPast?'past':cur?'current':'future'}" data-archive="${t.key}" ${!isPast&&!cur?'disabled':''}><span>${t.key.replace('-','.')}</span><h3>${t.name}</h3><p>${t.plant} · ${t.card}</p><b>${a?`${Math.round(a.pct)}% · 已冻结`:cur?'正在生长':'尚未开启'}</b></button>`}).join('');return `<section class="mgArchive"><div class="mgSheetHead"><small>MOON ARCHIVE</small><h2>月相收藏馆</h2><p>一年以后，这里会留下十二座不同的月下庭院。</p></div><div class="mgArchiveGrid">${cards}</div></section>`;}
  function cardModal(key){const t=theme(key),a=state.archives[key]||(()=>{const s=stats(key);return{...s,stage:stageFor(s.pct)}})();return `<div class="mgModal" data-close><article class="mgMoonCard"><div class="mgCardArt" style="--scene:url('${SCENE}')"></div><small>${key.replace('-','.')} · ${STAGES[a.stage||0].n}</small><h2>${t.name}</h2><p>${t.card}</p><div><span>${t.plant}</span><b>${Math.round(a.pct||0)}%</b></div></article></div>`;}
  function toast(msg){const t=app.querySelector('.mgToast');if(!t)return;t.textContent=msg;t.classList.add('show');clearTimeout(toast.t);toast.t=setTimeout(()=>t.classList.remove('show'),1800)}
  function escapeHtml(s){return String(s||'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
  app.addEventListener('click',e=>{if(e.target.closest('[data-exit]'))return exit();const v=e.target.closest('[data-view]');if(v){state.view=v.dataset.view;state.selectedDate=null;persist();render();return}const d=e.target.closest('[data-date]');if(d){state.selectedDate=d.dataset.date;persist();render();return}if(e.target.closest('[data-workbench]')){exit();if(typeof gotoPage==='function')gotoPage('today');return}const el=e.target.closest('[data-element]');if(el){const s=stats(monthKey()),u=unlocked(monthKey(),s.pct),x=u.find(y=>y.id===el.dataset.element);if(x)toast(`${x[0]} · 来自本月真实成长，${x[2]}% 时出现`);return}const a=e.target.closest('[data-archive]');if(a){app.insertAdjacentHTML('beforeend',cardModal(a.dataset.archive));return}if(e.target.closest('[data-freeze]')){const k=monthKey(),s=stats(k);state.archives[k]={key:k,total:s.total,pct:s.pct,active:s.active,focus:s.focus,stage:stageFor(s.pct),unlocked:unlocked(k,s.pct),frozenAt:today(),seed:deterministic(k,'garden')};persist();app.insertAdjacentHTML('beforeend',cardModal(k));return}if(e.target.closest('[data-close]'))e.target.closest('.mgModal')?.remove();});
  function enter(){document.querySelector('[data-page="game"]')?.replaceChildren(document.createTextNode('月相庭院'));document.body.classList.add('game-mode','moon-garden-active');app.classList.add('open');state.view='garden';render();}
  function exit(){app.classList.remove('open');document.body.classList.remove('game-mode','moon-garden-active');}
  enterGame=enter;leaveGame=exit;window.enterGameV5=enter;window.exitGameV5=exit;window.renderGameV5=render;
})();