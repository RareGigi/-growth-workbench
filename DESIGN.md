# 小小生长册 · Design

本文件是产品的唯一视觉设计权威。每轮审查按 Impeccable 的 `shape → critique → polish → adapt → harden` 执行。

## 视觉命题

清透的蓝紫色电子手账：冷静清爽的人物，带温度的环境与 UI。整体为现代生活 App、日程工具与轻收藏系统的交集，不做粉色少女网页、后台 Dashboard 或重度游戏大厅。

比例约为 80% 正式产品 UI + 20% 手账装饰。装饰只出现在场景、纸张、贴纸槽位和收藏卡面，不牺牲信息层级。

## 唯一设计令牌

| 角色 | 色值 | 使用方式 |
| --- | --- | --- |
| 画布 | `#f4f6fb` | 页面背景 |
| 纸张 | `#fffefa` | 主内容表面 |
| 雾蓝 | `#e8eefb` | 选中态与安静强调 |
| 淡蓝紫 | `#eeecf8` | 手账与收藏表面 |
| 暖杏 | `#f7f0eb` | 少量温暖区域 |
| 鼠尾草 | `#eaf2ed` | 习惯与运动反馈 |
| 主文字 | `#252c42` | 标题与正文 |
| 次文字 | `#4f5871` | 说明文字 |
| 主蓝 | `#6c88c8` | 主操作与进度 |
| 蓝紫 | `#7974ba` | 收藏与成长强调 |
| 雾粉 | `#c98f9c` | 极少量辅助强调 |

阴影只用两级低透明度蓝灰软阴影；主要表面圆角 16–20px，控件圆角 12px。禁止新增临时变量、页面专属视觉体系或覆盖式修复文件。

## 字体与信息层级

- UI、日期、回顾与月度标题统一使用系统无衬线字体栈；中文优先苹方 / 思源黑体回退，避免系统宋体在 iPhone 上产生突兀的印刷感。
- 固定使用五级文字角色：手机页面标题 26px / 700、区块标题 18px / 600、正文 15–16px / 400、辅助 13px / 400、元信息 12px / 500；正常信息不低于 12px。
- 中文标题字距归零，不使用不稳定的 650 / 750 字重；只有英文品牌小字允许正向字距。
- 页面标题不追求巨型营销字，手机端固定为 26px，优先让任务与行动进入视野。
- 输入框必须有可见边界与蓝色 `focus-visible` 焦点环。

## 布局

桌面端使用 252px 可折叠侧栏，收起为 72px；主内容最大宽度由阅读密度控制。项目仅存在于侧栏项目区，项目详情只展示当前项目。

900px 以下改为移动模式：固定侧栏变成唯一导航抽屉，顶部保留「项目」按钮，不再重复放置底部导航。页面底部、浮动加号和弹层都叠加 `env(safe-area-inset-bottom)`。

今日移动首屏控制为短顶部栏 + 172px 场景 + 紧凑重点任务；顺序固定为日期场景 → 今日重点 → 下一件事 → 快捷安排。任务、成长、手记使用三段页签切换，不把所有模块串成无限长页面。空状态高度约 116–160px，不制造半屏空白。

## 组件纪律

- 只为有明确语义的内容组使用卡片，不做卡片套卡片。
- 一句话添加与「给我方案」并列为一条紧凑行动栏；方案只要求选择时间、状态与方向，不先展示复杂表单。
- 三套方案在桌面并排比较，在手机横向吸附浏览，避免把弹层拉成长页面；每个选项保持独立的 44px 点击区域。
- 今日成长使用轻量横条列表，不拆成 KPI 卡。
- 任务完成仅显示轻提示，不弹庆祝大窗。
- 所有正式导航和操作使用同一套内联 SVG 描边图标。
- 手机操作热区至少 44×44px。
- 贴纸只能进入固定槽位，不开放自由拖拽铺满页面。
- 收藏页使用分段标签，避免一屏堆满衣橱、贴纸、徽章和纪念；衣橱首卡始终显示当前展示卡面，点按任意已拥有卡面即可原地切换、显示持久选中态并保存。
- 月度纪念页在手机首屏组合真实统计、固定贴纸位、所选徽章与一张已解锁角色卡面，表单和历史月份顺序下沉。

## 雨夜自习室

专注不是普通表单页，而是一间进入后仍然克制的场景：高清夜间书桌铺满空间，Canvas 只绘制低密度斜雨，真实计时与控制放在一张暖白纸面上。桌面端控制纸覆盖在场景右侧；900px 以下改为场景在上、纸面向上轻叠，390px 首屏能同时辨认环境、剩余时间和主要操作。

声音层只保留「窗雨」和「星雨琴音」两路。每路具有一个 44px 开关与独立音量，主开关用于试听或统一淡入 / 淡出。声音使用同源真实 MP3，由原生媒体元素在用户点击的同步调用栈内启动；界面只在 `play()` 真正成功后显示播放中，失败时给出可执行的媒体音量与重试提示。状态文字明确区分准备、连接、播放、失败与暂停；运行时隐藏移动端冗余页标题与浮动加号，避免打断自习氛围；离开页面立即停止声音，但计时器仍按绝对时间继续。

雨幕在 `prefers-reduced-motion` 下只绘制静态帧；设备像素比封顶以控制 iPhone GPU 成本。自习室只使用一张半透明控制纸，不把声音通道继续拆成大卡片，也不加入满屏粒子、视频背景或第三方音乐播放器。

## 手账材料系统

手账感来自结构和材料，不靠满屏装饰：画布使用极轻纸纤维；桌面侧栏像装订索引；页面标题只保留短而低对比的章节标记，不再给每个标题铺整条高光；今日重点、日记与周记使用真实横线纸资产；下一件事使用便签页签；收藏页使用装订纸与固定贴纸袋。装饰集中在记录、完成与收藏时刻，其余操作仍保持现代生活 App 的清晰度。

17 枚贴纸统一为 `200×200` 独立 SVG，使用白色模切边、蓝紫主色、奶油黄、雾粉和鼠尾草绿点缀，并保留轻微纸面投影。每枚必须有自己的主题构图，不能退化为放大的功能图标；不得内嵌文字，不得拼成 sprite，不得开放自由拖拽。

收藏槽位采用装订纸页、半透明贴纸袋与少量纸胶带标记。旋转只用于贴纸本身的 1–2° 静态差异，不改变网格、阅读顺序或点击区域。

## 数据保管

长期使用不能只依赖浏览器存储。「收藏 → 纪念」末尾提供一块克制的数据保管区，不增加侧栏入口。它显示当前真实内容摘要与最近保存时间，并提供导出、恢复两个明确动作。

导入文件后必须先显示任务、项目、专注、笔记、日记和月度纪念数量；替换当前设备数据前再次确认，并同时提供“先备份当前内容”。备份可能包含私人日记与笔记，界面始终提醒存放在自己的私密位置。手机按钮保持 44px 以上，恢复确认弹层不超过当前视口并处理底部安全区。

## 图像系统

| 类型 | 规格 | 策略 |
| --- | --- | --- |
| 今日场景 | 1672×941 WebP | 首屏 eager + high priority |
| 专注场景 | 1586×992 WebP | 进入专注页后 eager，固定高度与雨幕画布 |
| 收藏场景 | 1586×992 WebP | 非首屏 lazy |
| 人物身份图 | 1122×1402 WebP | 固定比例 |
| 限定卡面 | 853×1844 WebP，共 10 张 | 独立文件、统一卡面比例 |
| 贴纸 | 200×200 SVG，共 17 个 | 白色模切边、独立构图、固定收藏槽位 |
| 徽章 | 6 个独立 SVG | 正式徽章视觉 |

图片只承载场景与人物，不烤入日期、按钮、搜索框、Logo 或说明文字。所有 `<img>` 都提供 `width`、`height`、`decoding="async"`；非首屏增加 `loading="lazy"`。加载失败时由固定尺寸容器显示简洁占位。

## 卡面人物规则

人物保持同一身份：银灰发、蓝灰眼、清冷温和的成年男性面容与修长九头身方向。卡面不是换装贴图，所以姿势、相机距离、发型、手持、配饰和背景可以随主题变化；严禁把人物挤到近景造成头重脚轻，也不再用上衣 / 裤子 / 鞋子分层对齐。

古风、夏日、情人节、周年和庆典等主题使用原创服装语言与独立叙事场景，追求五星收藏卡面的完成度，但不复制第三方卡面构图、标识或文字。

## 动效与适配

- 抽屉与页面反馈使用 140–220ms 的轻微位移 / 淡入。
- 不使用满屏粒子、夸张弹窗或大量连续动画。
- 手机表单输入字号不低于 16px，避免 iPhone Safari 聚焦时自动缩放。
- `prefers-reduced-motion` 下将动效压缩至近乎即时。
- 断点重点验证：桌面、768px、430px、390px，以及 320–380px 的极窄兜底。

## Harden 清单

坏图不撑高；空数组有紧凑空状态；空复盘不渲染整屏零值报表；损坏存储、损坏备份与无效日期可恢复；旧数据仅迁移一次；计时器以绝对时间校正刷新误差；重复点击不重复发奖；同月反复保存不重复生成纪念轨迹；备份恢复必须先预览后确认；弹层锁定背景并圈定焦点；关闭的移动抽屉不可聚焦；浮动按钮避开 Safari 安全区与页面末尾内容。
