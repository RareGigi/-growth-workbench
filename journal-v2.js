const OLD_KEY='starGrowthCabin-v2';const KEY='growth-journal-v2';const LEGACY_KEY='growth-journal-v1';const dayKey=d=>{const z=new Date(d);return `${z.getFullYear()}-${String(z.getMonth()+1).padStart(2,'0')}-${String(z.getDate()).padStart(2,'0')}`};const TODAY=dayKey(new Date());const safeParse=(s,f={})=>{try{return JSON.parse(s)||f}catch{return f}};const old=safeParse(localStorage.getItem(OLD_KEY),'')||{};const legacy=safeParse(localStorage.getItem(LEGACY_KEY),'')||{};const defaults={version:2,tasks:{},plans:[],focus:[],study:[],english:[],podcasts:[],wordLog:[],body:{},reviews:{},journal:{},mood:{},coins:Number(old.coins||0),stars:Number(old.xp||0),owned:['cloud-set'],equipped:'cloud-set',stickers:['study-flower'],badges:[],monthly:{},planNotes:{},weeklyNotes:{},monthlyNotes:{},focusTimer:null,collectionTab:'closet'};let J={...defaults,...legacy,...safeParse(localStorage.getItem(KEY),'')};['tasks','body','reviews'].forEach(k=>{if(!Object.keys(J[k]||{}).length&&old[k])J[k]=old[k]});['focus','study','english','podcasts','wordLog'].forEach(k=>{if(!(J[k]||[]).length&&old[k])J[k]=old[k]});J.owned=Array.from(new Set(J.owned||['cloud-set']));J.stickers=Array.from(new Set(J.stickers||[]));J.badges=Array.from(new Set(J.badges||[]));if(!J.plans?.length)J.plans=[{id:'cpa',name:'CPA',area:'学习',color:'#ddd0e8',goal:'完成审计与经济法',next:'完成本周学习安排',weeklyGoal:300},{id:'novel',name:'第五时',area:'写作',color:'#f2d8dd',goal:'持续推进成熟正文',next:'完成下一段成熟正文',weeklyGoal:5000},{id:'body',name:'身体',area:'运动',color:'#dce8da',goal:'稳定运动与作息',next:'完成本周运动目标',weeklyGoal:3}];const save=()=>localStorage.setItem(KEY,JSON.stringify(J));const $=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));const uid=()=>Date.now()+Math.floor(Math.random()*999);function icon(n){const p={today:'M6 8h20v18H6z M10 5v6M22 5v6M6 13h20',plans:'M8 6h16v20H8z M12 10h8M12 15h8M12 20h5',focus:'M16 5a11 11 0 1 0 11 11M16 10v7l5 3',review:'M7 7h18v18H7z M11 12h10M11 17h10M11 22h6',collection:'M8 8h16v18H8z M12 8V5h8v3',coin:'M16 5c6 0 10 4 10 11s-4 11-10 11S6 23 6 16 10 5 16 5Z M13 13h6M13 18h6',star:'M16 5l3 7 8 1-6 5 2 8-7-4-7 4 2-8-6-5 8-1z',edit:'M8 24l2-6L22 6l4 4-12 12-6 2Z'};return `<svg viewBox="0 0 32 32"><path d="${p[n]||p.today}"/></svg>`}const pages=['today','plans','focus','review','collection'],pageNames={today:'今日',plans:'计划',focus:'专注',review:'复盘',collection:'收藏'};function nav(){return pages.map(x=>`<button data-page="${x}">${icon(x)}<span>${pageNames[x]}</span></button>`).join('')}function taskList(date=TODAY){return J.tasks[date]||(J.tasks[date]=[])}Object.values(J.tasks||{}).flat().forEach(x=>{if(!x.id)x.id=uid();if(x.focus===undefined)x.focus=false;if(!x.type)x.type='自定义';if(!x.estimate)x.estimate=25});save();function mascot(outfit=J.equipped,size='normal'){const map={'cloud-set':{body:'#eadff1',trim:'#8f799a',accent:'#f6dfe7',label:'云朵针织'},'sage-set':{body:'#dde9dc',trim:'#778f78',accent:'#f4e8cf',label:'鼠尾草散步'},'peach-set':{body:'#f5dfdf',trim:'#b67f8b',accent:'#eadff2',label:'桃雾周末'},'night-set':{body:'#dce4f1',trim:'#6f7895',accent:'#f5e9c7',label:'晚星阅读'}},c=map[outfit]||map['cloud-set'];return `<div class="mascot ${size==='small'?'small':''}" aria-label="原创兔系小形象，当前穿着${c.label}"><svg viewBox="0 0 180 220"><defs><linearGradient id="fur" x1="0" y1="0" x2="0" y2="1"><stop stop-color="#fffdf9"/><stop offset="1" stop-color="#f6efeb"/></linearGradient></defs><ellipse cx="90" cy="200" rx="48" ry="9" fill="#cbbec5" opacity=".18"/><path d="M62 55C43 27 51 10 68 34M118 55c19-28 11-45-6-21" fill="none" stroke="#8d8187" stroke-width="12" stroke-linecap="round"/><path d="M64 52C51 29 57 19 67 37M116 52c13-23 7-33-3-15" fill="none" stroke="#f0d9df" stroke-width="5" stroke-linecap="round"/><circle cx="90" cy="78" r="42" fill="url(#fur)" stroke="#eadfda" stroke-width="2"/><ellipse cx="76" cy="74" rx="4" ry="5" fill="#514b52"/><ellipse cx="104" cy="74" rx="4" ry="5" fill="#514b52"/><circle cx="74" cy="72" r="1.2" fill="#fff"/><circle cx="102" cy="72" r="1.2" fill="#fff"/><path d="M86 87q4 5 8 0" fill="none" stroke="#a5838c" stroke-width="2.5" stroke-linecap="round"/><ellipse cx="66" cy="87" rx="7" ry="3" fill="#f3d9de" opacity=".62"/><ellipse cx="114" cy="87" rx="7" ry="3" fill="#f3d9de" opacity=".62"/><path d="M56 124q34-21 68 0v56q-34 17-68 0z" fill="${c.body}" stroke="#d9ced4" stroke-width="2"/><path d="M60 126q30 20 60 0" fill="none" stroke="${c.trim}" stroke-width="5"/><path d="M90 128v52" stroke="${c.accent}" stroke-width="3" opacity=".7"/><path d="M58 139 42 164M122 139l16 25" stroke="#faf5f0" stroke-width="12" stroke-linecap="round"/><path d="M72 179v20M108 179v20" stroke="#faf5f0" stroke-width="13" stroke-linecap="round"/><circle cx="134" cy="159" r="7" fill="${c.accent}" stroke="${c.trim}" stroke-width="2"/></svg></div>`}function areaToday(){return [['学习',(J.study||[]).filter(x=>x.date===TODAY).reduce((s,x)=>s+(x.m||0),0),60,'分钟'],['工作',taskList().filter(x=>x.done&&['工作','职业','主线'].includes(x.type)).length,3,'件'],['运动',J.body[TODAY]?1:0,1,'次'],['写作',(J.wordLog||[]).filter(x=>x.date===TODAY).reduce((s,x)=>s+(x.n||0),0),1500,'字'],['英语',(J.english||[]).filter(x=>x.date===TODAY).reduce((s,x)=>s+(x.m||0),0),30,'分钟'],['播客',(J.podcasts||[]).filter(x=>x.date===TODAY).reduce((s,x)=>s+(x.m||0),0),30,'分钟']]}function todayTrail(){let a=[];taskList().filter(x=>x.done).forEach(x=>a.push([x.doneAt||Date.now(),`完成 ${x.t}`]));(J.focus||[]).filter(x=>x.date===TODAY).forEach(x=>a.push([x.ts||Date.now(),`专注 ${x.m} 分钟${x.label?' · '+x.label:''}`]));(J.study||[]).filter(x=>x.date===TODAY).forEach(x=>a.push([x.ts||Date.now(),`学习 ${x.m} 分钟${x.t?' · '+x.t:''}`]));(J.podcasts||[]).filter(x=>x.date===TODAY).forEach(x=>a.push([x.ts||Date.now(),`播客 ${x.m||0} 分钟${x.t?' · '+x.t:''}`]));(J.wordLog||[]).filter(x=>x.date===TODAY).forEach(x=>a.push([x.ts||Date.now(),`写作 ${x.n||0} 字`]));return a.sort((x,y)=>x[0]-y[0])}function taskRow(x){return `<div class="taskRow ${x.focus?'focus':''} ${x.done?'done':''}"><button class="taskCheck" data-toggle="${x.id}">${x.done?'✓':''}</button><div class="taskCopy"><div class="taskName">${esc(x.t)}</div><div class="taskMeta"><span class="areaPill">${esc(x.type||'自定义')}</span><span>${x.estimate||25} 分钟</span>${x.focus?'<span>今日重点</span>':''}</div></div><div class="taskMenu"><button data-focus-task="${x.id}" title="${x.focus?'取消重点':'设为重点'}">☆</button><button data-edit-task="${x.id}">${icon('edit')}</button><button data-delay="${x.id}">↗</button></div></div>`}function renderToday(){let list=taskList(),focus=list.filter(x=>x.focus).slice(0,3),normal=list.filter(x=>!x.focus),d=new Date(),date=d.toLocaleDateString('zh-CN',{month:'long',day:'numeric',weekday:'long'}),done=list.filter(x=>x.done).length;return `<div class="pageHead todayHead"><div><div class="dateStamp">${date}</div><h1>今天，也留下一点自己的痕迹。</h1><p>${done?`已经完成 ${done} 件事。慢慢来，今天已经有了形状。`:'不需要把一天塞满，先完成最重要的一小步。'}</p></div><button class="softButton" data-quick>快速记录</button></div><section class="paper todayHero"><div class="focusList"><div class="sectionHead"><h3>今日重点</h3><span>${focus.length}/3</span></div><div class="taskList">${focus.length?focus.map(taskRow).join(''):'<button class="emptyPrompt" data-quick><b>还没有重点任务</b><span>添加今天最值得完成的一件事</span></button>'}</div>${normal.length?`<div class="sectionHead secondary"><h3>普通任务</h3><span>${normal.length}</span></div><div class="taskList">${normal.map(taskRow).join('')}</div>`:''}</div><div class="mascotCard">${mascot()}<div class="mascotNote"><span class="tinyStar">✦</span>${done>=3?'今天已经很充实了。':'做完一件，就回来看看。'}</div></div></section><div class="todayGrid"><section class="paper block"><div class="blockHead"><b>今日成长</b><small>不比较，只记录</small></div><div class="growthRows">${areaToday().map(([n,v,g,u])=>`<div class="growthRow"><span>${n}</span><div class="growthBar"><i style="width:${Math.min(100,v/g*100)}%"></i></div><b>${v}<small>${u}</small></b></div>`).join('')}</div></section><section class="paper block"><div class="blockHead"><b>今日轨迹</b><small>${todayTrail().length} 条记录</small></div><div class="timeline">${todayTrail().length?todayTrail().slice(-8).map(([ts,t])=>`<div class="timelineItem"><time>${new Date(ts).toLocaleTimeString('zh-CN',{hour:'2-digit',minute:'2-digit',hour12:false})}</time><i></i><p>${esc(t)}</p></div>`).join(''):'<div class="softEmpty">第一条轨迹，会从你完成的一件小事开始。</div>'}</div></section></div><section class="paper dailyNote"><textarea id="dailyJournal" placeholder="给今天留一句话，不写也没关系。">${esc(J.journal[TODAY]||'')}</textarea><div class="moodBox"><b>今天的感觉</b><div class="moodChoices">${['安静','普通','开心','疲惫'].map((m,i)=>`<button data-mood="${i}" class="${J.mood[TODAY]===i?'active':''}">${m}</button>`).join('')}</div></div></section>`}function startOfWeek(){let d=new Date(),day=(d.getDay()+6)%7;d.setDate(d.getDate()-day);d.setHours(0,0,0,0);return d}function weekDays(){let s=startOfWeek();return Array.from({length:7},(_,i)=>{let d=new Date(s);d.setDate(s.getDate()+i);return dayKey(d)})}function planStats(p){let ds=weekDays();if(p.id==='cpa'){let all=J.study.reduce((s,x)=>s+(x.m||0),0),week=J.study.filter(x=>ds.includes(x.date)).reduce((s,x)=>s+(x.m||0),0);return{total:`${all} 分钟`,label:'累计学习',week,goal:p.weeklyGoal||300,unit:'分钟'}}if(p.id==='novel'){let all=J.wordLog.reduce((s,x)=>s+(x.n||0),0),week=J.wordLog.filter(x=>ds.includes(x.date)).reduce((s,x)=>s+(x.n||0),0);return{total:`${all.toLocaleString()} 字`,label:'累计写作',week,goal:p.weeklyGoal||5000,unit:'字'}}let all=Object.keys(J.body||{}).length,week=ds.filter(d=>J.body[d]).length;return{total:`${all} 天`,label:'身体记录',week,goal:p.weeklyGoal||3,unit:'次'}}function renderPlans(){return `<div class="pageHead"><div><h2>成长计划</h2><p>每一个长期目标，都像一本慢慢写满的成长册。</p></div><button class="softButton" data-new-plan>新建成长册</button></div><div class="plansGrid">${J.plans.map(p=>{let s=planStats(p),pct=Math.min(100,s.week/s.goal*100);return `<article class="paper planBook" style="--book:${p.color}"><div class="bookTab">${esc(p.area)}</div><div class="planBookTop"><div><small>${esc(p.area)} · GROWTH BOOK</small><h3>${esc(p.name)}</h3></div><button data-edit-plan="${p.id}">${icon('edit')}</button></div><p class="planGoal">${esc(p.goal)}</p><div class="planProgress"><div><i style="width:${pct}%"></i></div><span>本周 ${s.week.toLocaleString()} / ${s.goal.toLocaleString()} ${s.unit}</span></div><div class="planFooter"><div><small>${s.label}</small><b>${s.total}</b></div><div><small>下一步</small><b>${esc(p.next)}</b></div></div></article>`}).join('')}</div>`}let timer=null,left=25*60,total=25*60;function persistTimer(running=false){J.focusTimer={running,total,left,endAt:running?Date.now()+left*1000:null,label:$('#focusLabel')?.value||J.focusTimer?.label||''};save()}function runTimer(){clearInterval(timer);timer=setInterval(()=>{left=Math.max(0,left-1);updateTimerUI();if(left<=0){clearInterval(timer);timer=null;finishFocus()}},1000)}function restoreTimer(){if(J.focusTimer?.running&&J.focusTimer.endAt){total=J.focusTimer.total||1500;left=Math.max(0,Math.ceil((J.focusTimer.endAt-Date.now())/1000));if(left<=0)finishFocus();else runTimer()}else if(J.focusTimer){total=J.focusTimer.total||1500;left=J.focusTimer.left??total}}function updateTimerUI(){let t=$('#timerText');if(t)t.textContent=`${String(Math.floor(left/60)).padStart(2,'0')}:${String(left%60).padStart(2,'0')}`;let c=$('.timerCircle');if(c)c.style.setProperty('--timer',`${100-left/total*100}%`)}function renderFocus(){let today=J.focus.filter(x=>x.date===TODAY),sum=today.reduce((s,x)=>s+(x.m||0),0),label=J.focusTimer?.label||'';return `<div class="pageHead"><div><h2>专注室</h2><p>把世界调小一点，这一刻只做一件事。</p></div></div><section class="paper focusRoom"><div class="focusTopline"><input id="focusLabel" value="${esc(label)}" placeholder="这次要专注什么？"><span>今日 ${sum} 分钟 · ${today.length} 次</span></div><div class="timerCircle" style="--timer:${100-left/total*100}%"><b id="timerText">${String(Math.floor(left/60)).padStart(2,'0')}:${String(left%60).padStart(2,'0')}</b><span>FOCUS</span></div><div class="focusControls"><button class="primary" id="timerStart">${timer?'暂停':left<total?'继续':'开始'}</button><button id="timerReset">重置</button><select id="timerPreset"><option value="25" ${total===1500?'selected':''}>25 分钟</option><option value="45" ${total===2700?'selected':''}>45 分钟</option><option value="60" ${total===3600?'selected':''}>60 分钟</option></select></div><div class="focusDesk">${mascot(J.equipped,'small')}<div><small>专注中的小角落</small><b>${label?esc(label):'给这次专注起一个名字'}</b><p>完成后会自动加入今日轨迹，并获得金币与星星。</p><div class="deskDecor"><i></i><i></i><i></i></div></div></div></section>`}function weekStats(){let ds=weekDays(),tasks=ds.reduce((s,k)=>s+(J.tasks[k]||[]).filter(x=>x.done).length,0),focus=J.focus.filter(x=>ds.includes(x.date)).reduce((s,x)=>s+(x.m||0),0),study=J.study.filter(x=>ds.includes(x.date)).reduce((s,x)=>s+(x.m||0),0),words=J.wordLog.filter(x=>ds.includes(x.date)).reduce((s,x)=>s+(x.n||0),0),podcast=J.podcasts.filter(x=>ds.includes(x.date)).reduce((s,x)=>s+(x.m||0),0),active=ds.filter(d=>(J.tasks[d]||[]).some(x=>x.done)||J.journal[d]||J.reviews[d]||J.focus.some(x=>x.date===d)).length;return{tasks,focus,study,words,podcast,active,days:ds}}function monthStats(key=TODAY.slice(0,7)){let days=new Set(),taskDone=0;Object.entries(J.tasks).forEach(([d,a])=>{if(d.startsWith(key)){let n=a.filter(x=>x.done).length;if(n){days.add(d);taskDone+=n}}});J.focus.filter(x=>x.date?.startsWith(key)).forEach(x=>days.add(x.date));return{key,days:days.size,taskDone,focus:J.focus.filter(x=>x.date?.startsWith(key)).reduce((s,x)=>s+(x.m||0),0),study:J.study.filter(x=>x.date?.startsWith(key)).reduce((s,x)=>s+(x.m||0),0),words:J.wordLog.filter(x=>x.date?.startsWith(key)).reduce((s,x)=>s+(x.n||0),0),podcast:J.podcasts.filter(x=>x.date?.startsWith(key)).reduce((s,x)=>s+(x.m||0),0)}}function renderWeekReview(){let w=weekStats(),fields=[['完成任务',w.tasks,'件'],['专注时间',w.focus,'分钟'],['学习时间',w.study,'分钟'],['小说字数',w.words,'字'],['播客时间',w.podcast,'分钟'],['记录天数',w.active,'天']],most=[['学习',w.study],['写作',w.words/20],['专注',w.focus],['播客',w.podcast]].sort((a,b)=>b[1]-a[1])[0][0],wk=weekDays()[0];return `<section class="paper reviewPage"><div class="reviewCover"><small>WEEKLY JOURNAL</small><h3>这一周，慢慢写满了一页。</h3><p>本周最投入的领域是 <b>${most}</b>。不需要满格，只要还能看见自己走过的路。</p></div><div class="reviewSummary">${fields.map(([n,v,u])=>`<div><small>${n}</small><b>${Number(v).toLocaleString()} <em>${u}</em></b></div>`).join('')}</div><div class="reviewPrompts"><label>这一周最值得记住的事情<textarea id="weekMemory" placeholder="写下一件你不想忘记的事……">${esc(J.weeklyNotes['memory-'+wk]||'')}</textarea></label><label>下周三个重点<input id="weekNext" placeholder="例如：完成审计专题 / 运动3次 / 写3000字" value="${esc(J.weeklyNotes['next-'+wk]||'')}"></label></div></section>`}function renderMonthReview(){let m=monthStats(),name=new Date().toLocaleDateString('zh-CN',{year:'numeric',month:'long'});return `<section class="paper monthMemory"><div class="monthTape"></div><div class="monthHero"><small>MONTHLY MEMORY · ${m.key.replace('-','.')}</small><h3>${name}，留下来的东西。</h3><p>${m.days?`这个月已经有 ${m.days} 天留下记录。`:'这个月才刚刚开始。'}</p></div><div class="monthStatsLine"><span>任务 <b>${m.taskDone}</b></span><span>专注 <b>${m.focus}m</b></span><span>学习 <b>${m.study}m</b></span><span>写作 <b>${m.words}</b></span></div><div class="monthStickerSlots"><i>✦</i><i>花</i><i>记</i></div><label class="monthSummaryLabel">这个月，我最想留下的一句话<textarea id="monthMemory" placeholder="不用总结得很好，只要是你真的想记住的。">${esc(J.monthlyNotes[m.key]||'')}</textarea></label><div class="monthSignature">${mascot(J.equipped,'small')}<div><b>${m.words>=5000?'创作月':'日常月'}</b><small>当前造型 · 自动收入收藏册</small></div></div></section>`}function renderReview(){return `<div class="pageHead"><div><h2>复盘</h2><p>把真实发生过的事情，整理成好看的手账页。</p></div></div><div class="reviewTabs"><button class="active" data-review="week">本周手账</button><button data-review="month">月度纪念页</button></div><div id="reviewContent">${renderWeekReview()}</div>`}const outfits=[{id:'cloud-set',name:'云朵针织套装',price:0,tone:'雾紫',desc:'柔软、安静，适合普通日常。'},{id:'sage-set',name:'鼠尾草散步套装',price:90,tone:'鼠尾草绿',desc:'周末散步与运动日。'},{id:'peach-set',name:'桃雾周末套装',price:120,tone:'淡粉',desc:'轻松、明亮的一天。'},{id:'night-set',name:'晚星阅读套装',price:160,tone:'浅蓝',desc:'适合专注与夜间阅读。'}];const stickerDefs=[{id:'study-flower',name:'认真学习的小花',rule:()=>true,mark:'花',tone:'#f3dfe4'},{id:'focus-star',name:'第一颗专注星',rule:()=>J.focus.length>=1,mark:'星',tone:'#e9e1f3'},{id:'writer-note',name:'写作者便签',rule:()=>J.wordLog.reduce((s,x)=>s+(x.n||0),0)>=1000,mark:'写',tone:'#f4e6d5'},{id:'seven-days',name:'七日记录贴',rule:()=>Object.keys(J.journal).length>=7,mark:'7',tone:'#dfe9de'},{id:'podcast-ear',name:'听见世界',rule:()=>J.podcasts.length>=3,mark:'听',tone:'#dfeaf6'}];const badgeDefs=[{id:'first-focus',name:'第一次静下来',desc:'完成第一次专注',rule:()=>J.focus.length>=1},{id:'focus-10h',name:'十小时灯塔',desc:'累计专注10小时',rule:()=>J.focus.reduce((s,x)=>s+(x.m||0),0)>=600},{id:'task-30',name:'三十件小事',desc:'累计完成30个任务',rule:()=>Object.values(J.tasks).flat().filter(x=>x.done).length>=30},{id:'writer-5k',name:'纸上有风',desc:'累计写作5000字',rule:()=>J.wordLog.reduce((s,x)=>s+(x.n||0),0)>=5000},{id:'study-10h',name:'长灯未熄',desc:'累计学习10小时',rule:()=>J.study.reduce((s,x)=>s+(x.m||0),0)>=600},{id:'month-keeper',name:'月末留痕',desc:'写下第一篇月度总结',rule:()=>Object.values(J.monthlyNotes).some(Boolean)}];function syncRewards(){stickerDefs.forEach(s=>{if(s.rule()&&!J.stickers.includes(s.id))J.stickers.push(s.id)});badgeDefs.forEach(b=>{if(b.rule()&&!J.badges.includes(b.id))J.badges.push(b.id)});save()}function renderCloset(){let current=outfits.find(x=>x.id===J.equipped)||outfits[0];return `<div class="closetLayout"><section class="closetAvatar"><div class="avatarStage">${mascot()}<div class="outfitLabel"><b>${current.name}</b><small>${current.tone}</small></div></div><div class="closetActions"><button data-random>随机搭配</button><button data-equip="cloud-set">恢复初始</button></div></section><section><div class="collectionSectionHead"><div><b>小衣橱</b><small>整套造型，固定锚点，不做复杂分层。</small></div><span>${J.owned.length}/${outfits.length}</span></div><div class="itemGrid">${outfits.map(o=>{let owned=J.owned.includes(o.id),equipped=J.equipped===o.id;return `<article class="itemCard ${equipped?'equipped':''}"><div class="itemThumb">${mascot(o.id,'small')}</div><b>${o.name}</b><small>${o.desc}</small><div class="priceLine">${owned?'已拥有':`${o.price} 金币`}</div><button data-buy="${o.id}" ${equipped?'disabled':''}>${equipped?'正在穿':owned?'穿上':'兑换'}</button></article>`}).join('')}</div></section></div>`}function renderStickers(){return `<div class="collectionSectionHead"><div><b>贴纸册</b><small>奖励会自动进入固定槽位，不需要拖来拖去。</small></div><span>${J.stickers.length}/${stickerDefs.length}</span></div><div class="stickerGrid">${stickerDefs.map(s=>`<article class="sticker ${J.stickers.includes(s.id)?'':'locked'}" style="--sticker:${s.tone}"><i>${s.mark}</i><b>${s.name}</b><small>${J.stickers.includes(s.id)?'已收入':'继续生活，会遇见它'}</small></article>`).join('')}</div>`}function renderBadges(){return `<div class="collectionSectionHead"><div><b>成就徽章</b><small>有些成就只在真正发生以后才显示。</small></div><span>${J.badges.length}/${badgeDefs.length}</span></div><div class="badgeGrid">${badgeDefs.map((b,i)=>{let u=J.badges.includes(b.id);return `<article class="badge ${u?'':'locked'}"><div class="badgeMedal">${u?String(i+1).padStart(2,'0'):'?'}</div><b>${u?b.name:'未公开成就'}</b><small>${u?b.desc:'还没有抵达'}</small></article>`}).join('')}</div>`}function renderMonths(){let keys=new Set([...Object.keys(J.monthlyNotes||{}),TODAY.slice(0,7)]);return `<div class="collectionSectionHead"><div><b>月度手账</b><small>每个月会自动留下属于那个月的一页。</small></div><span>${keys.size} 页</span></div><div class="monthGrid">${[...keys].sort().reverse().map(k=>{let m=monthStats(k);return `<article class="monthCard"><span>${k.replace('-','.')}</span><h4>${k===TODAY.slice(0,7)?'正在书写':'已经收藏'}</h4><p>${m.days} 天记录 · ${m.focus} 分钟专注<br>${m.words.toLocaleString()} 字写作</p><b>${J.monthlyNotes[k]?'有一段月记':'等待一句月记'}</b></article>`}).join('')}</div>`}function renderCollection(){syncRewards();let tab=J.collectionTab||'closet',body=tab==='closet'?renderCloset():tab==='stickers'?renderStickers():tab==='badges'?renderBadges():renderMonths();return `<div class="pageHead"><div><h2>收藏</h2><p>现实生活里完成的事情，慢慢变成可以回看的小收藏。</p></div></div><div class="collectionTabs">${[['closet','小衣橱'],['stickers','贴纸册'],['badges','成就徽章'],['months','月度手账']].map(([id,n])=>`<button data-collection="${id}" class="${tab===id?'active':''}">${n}</button>`).join('')}</div><section class="paper collectionBody">${body}</section>`}function renderPage(page){const main=$('#journalMain');main.innerHTML=page==='today'?renderToday():page==='plans'?renderPlans():page==='focus'?renderFocus():page==='review'?renderReview():renderCollection();bindDynamic(page)}function renderApp(){document.body.innerHTML=`<div class="journalApp"><header class="journalTop"><div class="brandMark"><div class="brandSeal"><span></span></div><div class="brandText"><b>小小生长册</b><small>A LITTLE GROWTH JOURNAL</small></div></div><div class="wallet"><span>${icon('star')}<b id="starCount">${J.stars}</b></span><span>${icon('coin')}<b id="coinCount">${J.coins}</b></span></div></header><div class="journalShell"><aside class="journalSide"><nav class="sideNav">${nav()}</nav><div class="sideQuote">把普通的日子<br>一点一点收进自己的册子里。</div></aside><main id="journalMain" class="journalMain"></main></div><button class="quickAdd" data-quick aria-label="快速添加">+</button><div class="quickSheet" id="quickSheet"></div><nav class="mobileNav">${nav()}</nav><div id="journalToast" class="journalToast"></div></div>`;go('today')}function go(page){$$('[data-page]').forEach(b=>b.classList.toggle('active',b.dataset.page===page));renderPage(page);window.scrollTo({top:0,behavior:'smooth'})}function bindDynamic(){ $$('[data-toggle]').forEach(b=>b.onclick=()=>{let x=taskList().find(t=>String(t.id)===b.dataset.toggle);if(!x)return;x.done=!x.done;if(x.done&&!x.rewarded){x.doneAt=Date.now();x.rewarded=true;J.coins+=5;J.stars+=1;toast('+5 金币 · 今天又留下了一条轨迹')}save();renderPage('today')});$$('[data-focus-task]').forEach(b=>b.onclick=()=>{let x=taskList().find(t=>String(t.id)===b.dataset.focusTask);if(!x)return;if(!x.focus&&taskList().filter(t=>t.focus).length>=3)return toast('今日重点最多保留 3 件');x.focus=!x.focus;save();renderPage('today')});$$('[data-delay]').forEach(b=>b.onclick=()=>{let id=b.dataset.delay,x=taskList().find(t=>String(t.id)===id);if(!x)return;let d=new Date();d.setDate(d.getDate()+1);J.tasks[dayKey(d)]=J.tasks[dayKey(d)]||[];J.tasks[dayKey(d)].push({...x,id:uid(),done:false,focus:false,rewarded:false});J.tasks[TODAY]=taskList().filter(t=>String(t.id)!==id);save();toast('已经轻轻放到明天');renderPage('today')});$$('[data-edit-task]').forEach(b=>b.onclick=()=>editTask(b.dataset.editTask));$$('[data-mood]').forEach(b=>b.onclick=()=>{J.mood[TODAY]=Number(b.dataset.mood);save();renderPage('today')});let j=$('#dailyJournal');if(j)j.oninput=()=>{J.journal[TODAY]=j.value;save()};let start=$('#timerStart');if(start)start.onclick=()=>{if(timer){clearInterval(timer);timer=null;persistTimer(false);renderPage('focus')}else{J.focusTimer=J.focusTimer||{};J.focusTimer.label=$('#focusLabel')?.value||'';persistTimer(true);runTimer();renderPage('focus')}};let reset=$('#timerReset');if(reset)reset.onclick=()=>{clearInterval(timer);timer=null;left=total;J.focusTimer={total,left,running:false,label:$('#focusLabel')?.value||''};save();renderPage('focus')};let preset=$('#timerPreset');if(preset)preset.onchange=()=>{clearInterval(timer);timer=null;total=Number(preset.value)*60;left=total;J.focusTimer={total,left,running:false,label:$('#focusLabel')?.value||''};save();renderPage('focus')};let label=$('#focusLabel');if(label)label.oninput=()=>{J.focusTimer=J.focusTimer||{};J.focusTimer.label=label.value;save()};$$('[data-review]').forEach(b=>b.onclick=()=>{$$('[data-review]').forEach(x=>x.classList.toggle('active',x===b));$('#reviewContent').innerHTML=b.dataset.review==='week'?renderWeekReview():renderMonthReview();bindReviewInputs()});bindReviewInputs();$$('[data-collection]').forEach(b=>b.onclick=()=>{J.collectionTab=b.dataset.collection;save();renderPage('collection')});$$('[data-buy]').forEach(b=>b.onclick=()=>buyOrEquip(b.dataset.buy));$$('[data-equip]').forEach(b=>b.onclick=()=>{if(J.owned.includes(b.dataset.equip)){J.equipped=b.dataset.equip;save();renderPage('collection')}});let r=$('[data-random]');if(r)r.onclick=()=>{let owned=outfits.filter(o=>J.owned.includes(o.id));J.equipped=owned[Math.floor(Math.random()*owned.length)].id;save();renderPage('collection')};$$('[data-edit-plan]').forEach(b=>b.onclick=()=>editPlan(b.dataset.editPlan));let np=$('[data-new-plan]');if(np)np.onclick=newPlan;$$('[data-quick]').forEach(q=>q.onclick=openQuick)}function bindReviewInputs(){let a=$('#weekMemory'),b=$('#weekNext'),m=$('#monthMemory'),wk=weekDays()[0];if(a)a.oninput=()=>{J.weeklyNotes['memory-'+wk]=a.value;save()};if(b)b.oninput=()=>{J.weeklyNotes['next-'+wk]=b.value;save()};if(m)m.oninput=()=>{J.monthlyNotes[TODAY.slice(0,7)]=m.value;save();syncRewards()}}function finishFocus(){clearInterval(timer);timer=null;let mins=Math.round(total/60),label=J.focusTimer?.label||'未命名专注';J.focus.push({date:TODAY,m:mins,label,ts:Date.now()});J.focusTimer=null;left=total;J.coins+=Math.max(5,Math.round(mins/5));J.stars+=2;save();syncRewards();toast(`完成 ${mins} 分钟专注 · 已收入今日轨迹`);if($('#journalMain'))renderPage('focus')}function buyOrEquip(id){let o=outfits.find(x=>x.id===id);if(!o)return;if(J.owned.includes(id)){J.equipped=id;save();toast('已经换好了');renderPage('collection');return}if(J.coins<o.price)return toast(`还差 ${o.price-J.coins} 金币`);J.coins-=o.price;J.owned.push(id);J.equipped=id;save();toast(`已收藏「${o.name}」`);renderWallet();renderPage('collection')}function renderWallet(){let c=$('#coinCount'),s=$('#starCount');if(c)c.textContent=J.coins;if(s)s.textContent=J.stars}function editTask(id){let x=taskList().find(t=>String(t.id)===String(id));if(!x)return;openForm('编辑任务',`<label>任务标题<input id="formTitle" value="${esc(x.t)}"></label><div class="formPair"><label>成长领域<select id="formType">${['学习','工作','运动','写作','英语','播客','自定义'].map(v=>`<option ${x.type===v?'selected':''}>${v}</option>`).join('')}</select></label><label>预计时间<input id="formEstimate" type="number" min="5" value="${x.estimate||25}"></label></div><label class="checkLine"><input id="formFocus" type="checkbox" ${x.focus?'checked':''}> 设为今日重点</label>`,`保存`,()=>{x.t=$('#formTitle').value.trim()||x.t;x.type=$('#formType').value;x.estimate=Number($('#formEstimate').value)||25;x.focus=$('#formFocus').checked;save();closeQuick();renderPage('today')},()=>{J.tasks[TODAY]=taskList().filter(t=>String(t.id)!==String(id));save();closeQuick();renderPage('today')})}function newPlan(){openForm('新建成长册',`<label>名称<input id="planName" placeholder="例如：税务师"></label><div class="formPair"><label>成长领域<select id="planArea">${['学习','写作','运动','工作','英语','自定义'].map(x=>`<option>${x}</option>`).join('')}</select></label><label>每周目标<input id="planWeekly" type="number" value="180"></label></div><label>最终目标<input id="planGoal" placeholder="这本成长册想走到哪里"></label><label>下一步<input id="planNext" placeholder="下一件最具体的事"></label>`,`创建`,()=>{let name=$('#planName').value.trim();if(!name)return;J.plans.push({id:'p'+uid(),name,area:$('#planArea').value,color:['#ddd0e8','#f2d8dd','#dce8da','#dfeaf6'][J.plans.length%4],goal:$('#planGoal').value.trim()||'持续推进',next:$('#planNext').value.trim()||'完成下一步',weeklyGoal:Number($('#planWeekly').value)||180});save();closeQuick();renderPage('plans')})}function editPlan(id){let p=J.plans.find(x=>x.id===id);if(!p)return;openForm('编辑成长册',`<label>名称<input id="planName" value="${esc(p.name)}"></label><label>目标<input id="planGoal" value="${esc(p.goal)}"></label><label>下一步<input id="planNext" value="${esc(p.next)}"></label><label>每周目标<input id="planWeekly" type="number" value="${p.weeklyGoal||180}"></label>`,`保存`,()=>{p.name=$('#planName').value.trim()||p.name;p.goal=$('#planGoal').value.trim()||p.goal;p.next=$('#planNext').value.trim()||p.next;p.weeklyGoal=Number($('#planWeekly').value)||p.weeklyGoal;save();closeQuick();renderPage('plans')})}function openQuick(){openForm('快速添加',`<div class="quickChoices"><button data-q="task"><b>新建任务</b><span>加到今天</span></button><button data-q="focus"><b>开始专注</b><span>进入番茄钟</span></button><button data-q="journal"><b>写一句话</b><span>给今天留痕</span></button><button data-q="growth"><b>成长记录</b><span>学习 / 写作 / 播客</span></button></div>`,null,null);$$('[data-q]').forEach(b=>b.onclick=()=>{let t=b.dataset.q;if(t==='task')quickTask();else if(t==='focus'){closeQuick();go('focus')}else if(t==='journal'){closeQuick();go('today');setTimeout(()=>$('#dailyJournal')?.focus(),60)}else quickGrowth()})}function quickTask(){openForm('新建任务',`<label>任务标题<input id="formTitle" placeholder="今天要完成什么？"></label><div class="formPair"><label>成长领域<select id="formType">${['学习','工作','运动','写作','英语','播客','自定义'].map(v=>`<option>${v}</option>`).join('')}</select></label><label>预计时间<input id="formEstimate" type="number" min="5" value="25"></label></div><label class="checkLine"><input id="formFocus" type="checkbox"> 设为今日重点</label>`,`添加`,()=>{let t=$('#formTitle').value.trim();if(!t)return;if($('#formFocus').checked&&taskList().filter(x=>x.focus).length>=3)return toast('今日重点已经有 3 件');taskList().push({id:uid(),t,type:$('#formType').value,estimate:Number($('#formEstimate').value)||25,focus:$('#formFocus').checked,done:false});save();closeQuick();go('today')})}function quickGrowth(){openForm('记录成长',`<label>类型<select id="growthType"><option>学习</option><option>写作</option><option>播客</option><option>英语</option></select></label><label>数量<input id="growthValue" type="number" min="1" placeholder="分钟或字数"></label><label>备注<input id="growthNote" placeholder="今天做了什么"></label>`,`记录`,()=>{let type=$('#growthType').value,v=Number($('#growthValue').value),n=$('#growthNote').value.trim();if(!v)return;let row={date:TODAY,ts:Date.now()};if(type==='学习')J.study.push({...row,m:v,t:n});if(type==='写作')J.wordLog.push({...row,n:v});if(type==='播客')J.podcasts.push({...row,m:v,t:n});if(type==='英语')J.english.push({...row,m:v,n});J.coins+=3;J.stars+=1;save();closeQuick();toast('已经记进今天的手账');go('today')})}function openForm(title,body,submitText='保存',onSubmit=null,onDelete=null){let sheet=$('#quickSheet');sheet.innerHTML=`<div class="quickPanel"><div class="sheetHandle"></div><div class="sheetHead"><h3>${title}</h3><button data-close>×</button></div>${body}${submitText?`<div class="sheetActions">${onDelete?'<button class="dangerSoft" data-delete>删除</button>':''}<button class="primaryWide" data-submit>${submitText}</button></div>`:''}</div>`;sheet.classList.add('open');sheet.onclick=e=>{if(e.target===sheet)closeQuick()};$('[data-close]').onclick=closeQuick;if(onSubmit)$('[data-submit]').onclick=onSubmit;if(onDelete)$('[data-delete]').onclick=onDelete}function closeQuick(){let s=$('#quickSheet');if(s){s.classList.remove('open');setTimeout(()=>s.innerHTML='',150)}}function toast(msg){let t=$('#journalToast');if(!t)return;t.textContent=msg;t.classList.add('show');clearTimeout(toast.t);toast.t=setTimeout(()=>t.classList.remove('show'),1900);renderWallet()}renderApp();restoreTimer();document.addEventListener('click',e=>{let p=e.target.closest('[data-page]');if(p)go(p.dataset.page)});window.addEventListener('beforeunload',()=>{if(timer)persistTimer(true)});